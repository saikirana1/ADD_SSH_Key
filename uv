step 1
install python
2
curl -LsSf https://astral.sh/uv/install.sh | sh
3)
for project uv add
4)
for install alredy 
uv sync
5) run the server
 uv run uvicorn index:app --reload
6)activate and install virtual env
      python3 -m venv venv 
      source venv/bin/activate   


for fast api
  1) uv init
  2) uv add 
  3)source .venv/bin/activate
  4)uv run uvicorn index:app --reload
